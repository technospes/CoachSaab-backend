name: Keep Render Alive
on:
  schedule:
    - cron: '*/3 3-17 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Health Endpoint
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            https://coachsaab-api.onrender.com/api/v1/health
